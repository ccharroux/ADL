function getSearchData(searchType) {
    //get session object
    //switch statement on token
    //make api call can return the data
    //turn into json that the datatable can understand
    switch(searchType.toUpperCase()) {
        case "":
            break;
        default:
    }

    //make api call and return data

}